[build]
  base = "app2-StudyHinter/frontend"
  command = "npm run build"
  publish = "dist"

# Forward backend API calls to Railway
[[redirects]]
  from = "/api/*"
  to = "https://kiroween-production-3af6.up.railway.app/api/:splat"
  status = 200
  force = true

# SPA routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
